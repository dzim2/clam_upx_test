
this test contains the bytes necessary to trigger mydoom detections in PE 
and mirai in ELF files. These were used as an unpack test in libclamav 
without requiring any special sigs

test.yara is just to make sure the compiler didnt bork my bytes. 